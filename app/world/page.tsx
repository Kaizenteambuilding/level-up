import DemoWorld from '@/components/DemoWorld'
import GuestFeedback from '@/components/GuestFeedback'

export default function WorldPage() {
  return <main className="shell game-shell"><DemoWorld /><GuestFeedback /></main>
}
